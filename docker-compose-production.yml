version: '3.8'

services:
  ingress-proxy:
    image: ingress-proxy-production:local
    build:
      context: .
      dockerfile: Production.dockerfile
      target: ingress-proxy-production
    ports:
      - "80:80"
      - "443:443"
